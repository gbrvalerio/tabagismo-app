import React from 'react';
import { View } from 'react-native';

export function IconSymbol({
  size,
  color,
  name,
}: {
  size?: number;
  color?: string;
  name: string;
}) {
  return <View />;
}
